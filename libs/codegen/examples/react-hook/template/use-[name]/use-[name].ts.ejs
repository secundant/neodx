import { useState } from 'react';

export interface Use<%= $.toCase(name, 'CaSe') %>Params {
  initialValue: string;
}

export function use<%= $.toCase(name, 'CaSe') %>({ initialValue }: Use<%= $.toCase(name, 'CaSe') %>Params) {
    const [value, setValue] = useState(initialValue);

    return [value, setValue];
}
