# @neodx/autobuild

> **Retired from the neodx critical path (WP-V2 / S3).**
> Package is `private`, ignored by Changesets, and not published.
> Build publishable libraries with `vp pack` (see root [`AGENTS.md`](../../AGENTS.md)).
> Tracking deletion: [#162](https://github.com/secundant/neodx/issues/162).

Historical Rollup + SWC library builder kept in-tree until #162 closes. Do not install it as a
dependency of new packages. Do not treat the source under this directory as a supported product
surface.
